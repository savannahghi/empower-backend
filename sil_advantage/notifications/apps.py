"""Notifications app config."""
from django.apps import AppConfig


class NotificationsConfig(AppConfig):
    """Notifications app config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "sil_advantage.notifications"
