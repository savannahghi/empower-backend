"""Segments app config."""
from django.apps import AppConfig


class SegmentsConfig(AppConfig):
    """Segments app config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "sil_advantage.segments"
